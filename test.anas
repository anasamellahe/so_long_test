#include "mlx.h"
#include <stdio.h>
#include <stdlib.h>

// int main ()
// {
// int x, y;
// void *mlx = mlx_init();
// void *win = mlx_new_window(mlx, 100, 100, "anas");
// void *img = mlx_xpm_file_to_image(mlx, "img/apple/apple1.xpm", &x, &y);
// if (!img)
//     printf("test\n");

// mlx_destroy_image(mlx, img);
// mlx_destroy_window(mlx, win);
// mlx_destroy_display(mlx);
// free(mlx);
// printf("hello\n");

// }

int main ()
{
    void *mlx = mlx_init();
    void *win = mlx_new_window(mlx, 720, 720, "anas");
    mlx_string_put(mlx, win, 10, 10, 189171196, "00000000000000000000000"); 
    mlx_loop(mlx);
}